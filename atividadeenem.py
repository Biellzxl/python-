#enem
 def ordenacao(vetor):
     for i in range(len(vetor)):
         for j in range(j+1, len(vetor)):
             if vetor[i] > vetor[j]
                 x = vetor[j]
                 vetor[i] = vetor[j]
                 vetor[j] = x

